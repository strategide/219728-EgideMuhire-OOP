package beginner;

/**
 * A 'Child' class that directly inherits from a 'Parent' class.
 * 
 * - A 'child' object is an instance of the 'Child' class.
 * 
 * Because the 'Child' class inherits from the 'Parent' class, any instance of
 * the 'Child' class is also an instance of the 'Parent' class.
 * 
 * Because the 'Parent' class inherits from the 'GrandParent' class, any 
 * instance of the 'Child' class is also an instance of the 'GrandParent' class 
 * (through 'Parent').
 * 
 * @author strategide
 */
public class Child extends Parent { // 'extends' means 'inherits from'
    
}
