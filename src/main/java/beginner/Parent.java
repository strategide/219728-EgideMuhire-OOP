package beginner;

/**
 * A 'Parent' class that directly inherits from a 'GrandParent' class.
 * 
 * - A 'parent' object is an instance of the 'Parent' class.
 * 
 * Because the 'Parent' class inherits from the 'GrandParent' class, any instance of
 * the 'Parent' class is also an instance of the 'GrandParent' class.
 * 
 * Because the 'Parent' class does not inherit from the 'Child' class, any 
 * instance of the 'Parent' class is NOT an instance of the 'Child' class.
 * 
 * @author strategide
 */
public class Parent extends GrandParent { // 'extends' means 'inherits from'
    
}
