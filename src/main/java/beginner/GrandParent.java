package beginner;

/**
 * A 'GrandParent' class that does not directly inherit from any class.
 * 
 * - A 'grandParent' object is an instance of the 'GrandParent' class.
 * 
 * Because the 'GrandParent' class does not inherit from the 'Parent' class, any 
 * instance of the 'GrandParent' class is NOT an instance of the 'Parent' class.
 * 
 * Because the 'GrandParent' class does not inherit from the 'Child' class, any 
 * instance of the 'GrandParent' class is NOT an instance of the 'Child' class.
 * 
 * @author strategide
 */
public class GrandParent { // does not extend any class directly
    
}
